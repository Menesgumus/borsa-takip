{
  "name": "borsa-takip-root",
  "private": true,
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "build:frontend": "pnpm --filter frontend build",
    "test:frontend": "pnpm --filter frontend test:unit"
  },
  "packageManager": "pnpm@9.12.0",
  "pnpm": {
    "overrides": {
      "vite": ">=8.2.2",
      "postcss": ">=8.5.28"
    }
  }
}

